buildscript {
  apply plugin: 'maven'

  repositories {
    mavenLocal()
    mavenCentral()
  }
  
  dependencies {
    classpath 'org.akhikhl.gretty:gretty-plugin:0.0.4'
  }
}

apply plugin: 'base' // add "clean" task to the root project.

ext {
  groovy_version = '2.2.0'
}

subprojects {
  apply plugin: 'maven'

  repositories {
    mavenLocal()
    mavenCentral()
  }
}
