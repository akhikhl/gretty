apply plugin: 'java'

ext {
  jetty9_servlet_api_version = '3.1.0'
}

repositories {
  mavenLocal()
  jcenter()
}

dependencies {
  compile "javax.servlet:javax.servlet-api:$jetty9_servlet_api_version"
}

