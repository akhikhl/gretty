apply from: rootProject.file('libs/common.gradle')
apply from: rootProject.file('libs/publish.gradle')

import org.apache.tools.ant.filters.*

dependencies {
  compile project(':libs:gretty-plugin-commons')
  compile "org.eclipse.jetty:jetty-util:$jetty7_version"
  compile gradleApi()
}

processResources {
  filter ReplaceTokens, tokens: [
    jettyVersion: project.jetty7_version
  ]
}
