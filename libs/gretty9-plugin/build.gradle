import org.apache.tools.ant.filters.*

apply plugin: 'groovy'

dependencies {
  compile project(':libs:gretty-plugin-commons')
  compile "org.eclipse.jetty:jetty-util:$jetty9_version"
  compile gradleApi()
}

processResources {
  filter ReplaceTokens, tokens: [
    jettyVersion: project.jetty9_version
  ]
}
